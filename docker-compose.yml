services:

  db-webdinamis:
    image: mariadb:lts
    environment:
      MARIADB_ROOT_PASSWORD: Admin@8419
      MARIADB_DATABASE: dbcompro_2388010009
      MARIADB_USER: userwebdinamis_2388010009
      MARIADB_PASSWORD: usrcompro_2388010009
    volumes:
      - db_data:/var/lib/mysql
      - /home/ubuntu/docker-aws/db_data/fashion_db.sql:/docker-entrypoint-initdb.d/fashion_db.sql:ro
    restart: always
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - uasnet

  container-statis:
    image: reviinjani/uas_statis_2388010009:latest
    ports:
      - "80:80"
    restart: always
    networks:
      - uasnet

  container-dinamis:
    image: reviinjani/uas_dinamis_2388010009:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      DB_USER: userwebdinamis_2388010009
      DB_PASSWORD: usrcompro_2388010009
      DB_NAME: dbcompro_2388010009
      DB_HOST: db-webdinamis
      AUTH_TRUST_HOST: "true"
      NEXTAUTH_SECRET: adm-serv-web-bebas-saja-32-karakter
      NEXTAUTH_URL: http://13.239.35.21:3000
    depends_on:
      db-webdinamis:
        condition: service_healthy
    networks:
      - uasnet

volumes:
  db_data: {}

networks:
  uasnet:
    driver: bridge
